// Defaults for the legacy local app. The master gateway serves this URL with
// its configured public server name, and never includes credentials.
globalThis.BEEPA_MASTER_CONFIG = { serverName: 'master' };
