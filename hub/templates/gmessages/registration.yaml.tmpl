id: gmessages
url: http://mautrix-gmessages:29336
as_token: ${AS_TOKEN_GMESSAGES}
hs_token: ${HS_TOKEN_GMESSAGES}
sender_localpart: neGjYF32GPICYID4IbB2VPB0vmfK4P7u
rate_limited: false
namespaces:
    users:
        - regex: ^@gmessagesbot:localhost$
          exclusive: true
        - regex: ^@gmessages_.*:localhost$
          exclusive: true
        # Double-puppeting: let the bridge act as the human user (non-exclusive
        # so Synapse still owns the real @jkali account) so the user's OWN sent
        # Google Messages render as "you", not as a foreign ghost. Pairs with
        # double_puppet.secrets.localhost (as_token) in config.yaml.
        - regex: ^@jkali:localhost$
          exclusive: false
de.sorunome.msc2409.push_ephemeral: true
receive_ephemeral: true
